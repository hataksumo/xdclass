server:
  port: 8081
  servlet:
    context-path: /

spring:
  application:
    name: user1
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        namespace: xdclass
        name: user
        file-extension: yml
        group: LOCAL
      discovery:
        namespace: xdclass
        register-enabled: true